# === MySQL (docker-compose가 읽음) ===
DB_ROOT_PASSWORD=rootpass
DB_NAME=squishy
DB_USER=squishy
DB_PASSWORD=squishypass

# === 접속 컨텍스트 ===
# CI3 (web 컨테이너 내부) → DB_HOST=db
# Python 크롤러 (호스트)  → DB_HOST=127.0.0.1
DB_HOST=127.0.0.1
DB_PORT=3306
